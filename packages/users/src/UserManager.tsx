/** @format */

import { useSyncNavigationFilters } from '@mui-toolpad-extended-tuni/main';

export const UserManager = () => {
  // Use the sync hook to keep navigation filters in sync with user preferences
  useSyncNavigationFilters();

  // ...existing code...
  return null;
};

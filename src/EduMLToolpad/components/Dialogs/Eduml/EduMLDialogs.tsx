import CourseSettings from "./CourseSettings";

/**
 * @description The main component for the EduML dialogs, aka dialogs specific to EduML main frame and not invidual microservices (e.g. EduChat, EsManager, etc.)
 * @returns JSX.Element
 */
const EduMLDialogs = () => {
  return <CourseSettings />;
};

export default EduMLDialogs;
